---
apiVersion: v1
kind: Service
metadata:
  name: myservice
  labels:
    app: myapp
  namespace: mydeployment-02
spec:
  type: LoadBalancer
  selector:
    app: myapp
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
    
